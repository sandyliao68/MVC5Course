@*@section breadcrumb {
     <li><a href="#">@ViewBag.Title</a></li>
    }
*@

@Html.ActionLink(" ", "Index", null, new { @class="btn btn-primary",data_name="SANDY"})
<a href="@Url.Action("Index")" class ="btn btn-primary" data-name="SANDY">超連結</a>

    @{
        ViewBag.Title = "About";

    }
    <h2>@ViewBag.Title.</h2>
    <h3>@ViewBag.Message</h3>
    <h3>@ViewBag.執行時間</h3>

        <p>sandy Use this area to provide additional information.</p>
